[package]
name = "a04_stylance_test"
version = "0.1.0"
edition = "2021"

[dependencies]
leptos = { version = "0.6.15", features = ["nightly", "csr"] }
stylance = { version = "0.5.1", features = ["nightly"] }
